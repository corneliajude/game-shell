const initialState = {
    // unusued
    playing: true, // false
};

// ------------------------------------> action = { type, payload }
export const gameReducer = (state = initialState, { type, payload }) => {
    return state;
};